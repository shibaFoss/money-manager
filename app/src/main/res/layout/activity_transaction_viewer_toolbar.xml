<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="58dp"
                android:background="?attr/colorPrimary"
                android:orientation="vertical">

    <TextView
        android:id="@+id/txt_toolbar"
        style="@style/textTitleStyle"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toEndOf="@+id/bnt_back"
        android:layout_toStartOf="@+id/bnt_delete"
        android:gravity="center_vertical"
        android:text="@string/transaction"
        android:textColor="@color/white_text"
        android:textStyle="bold"
        />

    <ImageButton
        android:id="@+id/bnt_back"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="?attr/selectableItemBackground"
        android:onClick="onBack"
        android:padding="@dimen/dp_10"
        android:src="@drawable/ic_toolbar_back"
        />

    <ImageButton
        android:id="@+id/bnt_edit"
        android:layout_toStartOf="@+id/bnt_delete"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="?attr/selectableItemBackground"
        android:onClick="onEditTransaction"
        android:padding="@dimen/dp_10"
        android:src="@drawable/ic_toolbar_edit_mode"
        />

    <ImageButton
        android:id="@+id/bnt_delete"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:background="?attr/selectableItemBackground"
        android:onClick="onDeleteTransaction"
        android:padding="@dimen/dp_10"
        android:src="@drawable/ic_toolbar_delete"
        />

</RelativeLayout>
